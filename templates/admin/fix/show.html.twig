{% extends 'base.html.twig' %}

{% block title %}Fix{% endblock %}

{% block body %}
    <h1>Fix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idfix</th>
                <td>{{ fix.idfix }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fix.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fix_index') }}">back to list</a>

    <a href="{{ path('fix_edit', {'idfix': fix.idfix}) }}">edit</a>

    {{ include('admin/fix/_delete_form.html.twig') }}
{% endblock %}
